name: weather forecast production cd

on:
  workflow_run:
    workflows: ["weather forecast ci"]
    types: [completed]
    branches: [main]

permissions:
      id-token: write
      contents: read

jobs:
  debug:
    runs-on: ubuntu-latest
    environment: Production
    
    steps:
      - name: Show selected environment
        run: echo "Environment is ${{ vars.ENVIRONMENT_NAME }}"

      - name: Check Azure secret presence
        run: |
          [ -n "${{ secrets.AZURE_CLIENT_ID }}" ] && echo "CLIENT_ID present" || echo "CLIENT_ID missing"
          [ -n "${{ secrets.AZURE_TENANT_ID }}" ] && echo "TENANT_ID present" || echo "TENANT_ID missing"
          [ -n "${{ secrets.AZURE_SUBSCRIPTION_ID }}" ] && echo "SUBSCRIPTION_ID present" || echo "SUBSCRIPTION_ID missing"

  deploy-to-production-webapp:
    environment: Production
    uses: ./.github/workflows/reusable-deploy-azure-webapp.yml
    with:
      environment_name: Production
      artifact_name: dotnet-app
      artifact_path: ./dotnet-app
      source_run_id: ${{ github.event.workflow_run.id }}
      webapp_name: weatherforecastpipelineprod
    secrets: 
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}





  #   runs-on: ubuntu-latest
  #   environment: Production

  #   steps:
  #     - name: Download artifact from CI workflow
  #       uses: actions/download-artifact@v4
  #       with:
  #         name: dotnet-app
  #         path: ./dotnet-app
  #         run-id: ${{ github.event.workflow_run.id }}
  #         github-token: ${{ github.token }}

  #     - name: Azure login
  #       uses: azure/login@v2
  #       with:
  #         client-id: ${{ secrets.AZURE_CLIENT_ID }}
  #         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
  #         subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

  #     - name: Azure Web App deploy
  #       uses: azure/webapps-deploy@v3
  #       with:
  #         app-name: weatherforecastpipelineprod
  #         package: ./dotnet-app
#////////
  #   if: >
  #     github.event.workflow_run.event == 'push' &&
  #     github.event.workflow_run.conclusion == 'success' &&
  #     github.event.workflow_run.head_branch == 'staging'
  #   environment: Staging
  #   uses: ./.github/workflows/reusable-deploy-azure-webapp.yml
  #   with:
  #     environment_name: Staging
  #     artifact_name: dotnet-app
  #     artifact_path: ./dotnet-app
  #     source_run_id: ${{ github.event.workflow_run.id }}
  #     webapp_name: weatherforecastpipelineapp
  #   secrets: 
  #     AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
  #     AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
  #     AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}




      